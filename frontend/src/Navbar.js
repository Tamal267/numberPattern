import React from 'react'

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>Number Pattern</h1>
      <sub>(digits(.digits)?(E[+-]?digits)?)</sub>
    </nav>
  )
}

export default Navbar
